# Spectra KWS — ESP-IDF Firmware
# Target: Seeed XIAO ESP32-C5 (RISC-V, Wi-Fi 6)

cmake_minimum_required(VERSION 3.16)

# Pull in ESP-IDF build system
include($ENV{IDF_PATH}/tools/cmake/project.cmake)

project(spectra-kws)
